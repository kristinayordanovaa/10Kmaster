# Netlify redirect rules

# Blog posts use Edge Function for server-side rendering (SSR) - critical for SEO
# Edge function renders posts server-side so Google sees full content
/blog/:slug  /.netlify/edge-functions/blog-post-ssr  200

# Fallback for blog posts without slug
/blog  /blog.html  200
